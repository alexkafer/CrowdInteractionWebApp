import { ModeDisplayDirective } from './mode-display.directive';

describe('ModeDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ModeDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
